import { describe, it, expect } from 'vitest';
import CampaignJournalPanel from '../CampaignJournalPanel';

describe('CampaignJournalPanel', () => {
  it('should be imported correctly', () => {
    expect(CampaignJournalPanel).toBeDefined();
  });
});
