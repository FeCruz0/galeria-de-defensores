import { describe, it, expect } from 'vitest';
import OnlineMembersBar from '../OnlineMembersBar';

describe('OnlineMembersBar', () => {
  it('should be defined', () => {
    expect(OnlineMembersBar).toBeDefined();
  });
});
