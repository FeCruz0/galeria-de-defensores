import { describe, it, expect } from 'vitest';
import TableChatPanel from '../../TableChatPanel';

describe('TableChatPanel', () => {
  it('should be imported correctly', () => {
    expect(TableChatPanel).toBeDefined();
  });
});
