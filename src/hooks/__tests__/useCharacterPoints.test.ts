import { describe, it, expect } from 'vitest';
import { useCharacterPoints } from '../useCharacterPoints';

describe('useCharacterPoints', () => {
  it('should be defined', () => {
    expect(useCharacterPoints).toBeDefined();
  });
});
