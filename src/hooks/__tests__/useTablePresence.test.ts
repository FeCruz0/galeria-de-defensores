import { describe, it, expect } from 'vitest';
import { useTablePresence } from '../useTablePresence';

describe('useTablePresence', () => {
  it('should be defined', () => {
    expect(useTablePresence).toBeDefined();
  });
});
