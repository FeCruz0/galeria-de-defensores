import { describe, it, expect } from 'vitest';
import { useVttSession } from '../useVttSession';

describe('useVttSession', () => {
  it('should be defined', () => {
    expect(useVttSession).toBeDefined();
  });
});
